<h1 class="jumbotron">Skraper</h1>

<div class="container">
    <div class="row">
        {{#if event includeZero=true }}
            {{#each event}}
                {{> events/event-card}}
            {{/each}}
        {{/if}}
    </div>
</div>